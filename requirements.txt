# Python package dependencies
git+https://github.com/yt-dlp/yt-dlp.git

# Note: ffmpeg is required by the downloader but is a system dependency,
# not a pip package. Install ffmpeg separately and ensure it is on PATH.
